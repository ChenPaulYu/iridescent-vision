# Dev-only flags (Vite loads this file for `npm run dev` only; production
# builds never see it, so the debug UI cannot be summoned on the deployed
# site even with ?debug in the URL).
VITE_DEBUG_UI=true
